package com.example.convertAPI.model.repository;

import com.example.convertAPI.model.entity.FinalFileEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FinalFileEntityRepository extends JpaRepository<FinalFileEntity, Long> {
}
